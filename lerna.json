{
  "packages": ["packages/*"],
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "publish": {
        "ignoreChanges": ["**/*.md", "**/test/**"]
      }
    }
  }
}
